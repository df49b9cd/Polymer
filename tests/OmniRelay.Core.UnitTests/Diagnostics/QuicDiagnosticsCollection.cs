using Xunit;

namespace OmniRelay.Core.UnitTests.Diagnostics;

[CollectionDefinition(nameof(QuicDiagnosticsCollection), DisableParallelization = true)]
public sealed class QuicDiagnosticsCollection
{
}
