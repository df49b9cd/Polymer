# OmniRelay.ControlPlane.Abstractions

Shared control-plane contracts (event bus, drain participants, TLS options, throttling signals, membership snapshots) consumed by OmniRelay.ControlPlane and data-plane hosts.
