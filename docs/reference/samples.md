# OmniRelay Samples

The legacy `samples/` directory was removed while we refresh the runtime for Native AOT and source-generated configuration. New sample hosts will be added once the refactor stabilizes. Until then, use the hosting examples in `README.md` and the dispatcher configuration guidance under `docs/`.
